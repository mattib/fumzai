// This script runs in the isolated world and can read extension settings
// For MVP, the inject script will use hardcoded time limits, but this is the bridge if needed.
console.log("[FumzAI] Content script loaded.");
